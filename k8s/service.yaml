apiVersion: v1
kind: Service
metadata:
  name: boxstore-api-service
spec:
  type: NodePort
  selector:
    app: boxstore-api
  ports:
    - port: 8008
      targetPort: 8008
      nodePort: 30008
